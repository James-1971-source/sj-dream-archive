# S&J 드림 아카이브 보안 취약점 진단 및 조치 결과 보고서

본 문서는 S&J 드림 아카이브 로컬 개발 PC와 GitHub 원격 저장소 간의 소스 동기화 상태 분석 중 발견된 보안 취약점의 상세 내용과 이를 해결하기 위해 수행한 무중단 보안 조치 내역을 정리한 보고서입니다.

---

## 1. 보안 문제 발생 내용 (취약점 진단)

* **발견 배경**: 로컬 PC 소스([SJ_DATABASE](./))와 GitHub 원격 저장소 간 동기화 진단 중 발견.
* **보안 취약점**: 
  * Supabase URL과 데이터베이스 접근용 API 키(ANON KEY) 정보가 담긴 `.env` 파일이 Git 추적 대상(`tracked`)으로 등록되어 있었음.
  * S&J 드림 아카이브 GitHub 저장소가 **퍼블릭(Public, 전체 공개)**으로 설정되어 있어, 외부 사용자가 깃허브 커밋 기록(History)을 통해 구형 API 키(JWT 기반 `eyJ...`로 시작)를 자유롭게 획득할 수 있는 상태였음.
  * 만약 Supabase에 RLS(Row Level Security, 행 수준 보안) 설정이 미비할 경우 외부인이 임의로 데이터를 조회, 위조, 삭제할 수 있는 높은 위험성이 잠재해 있었음.

---

## 2. 보안 취약점 해결 조치 과정

서비스 이용자(청소년들)가 활발히 접속하여 작품을 이용 및 업로드하는 중이었으므로, 서비스 일시 중단이나 동작 오류가 전혀 발생하지 않는 **무중단 교체 프로세스**를 기획하여 안전하게 진행했습니다.

### 단계 1: 환경 변수(`.env`) 파일 Git 추적 안전 제거 및 예외 설정
1. **`.gitignore` 수정**: 
   * [`.gitignore`](./.gitignore) 파일 최하단에 `# Local env files` 섹션과 `.env`를 등록하여 환경 변수 파일이 앞으로 Git에 절대 감지되거나 업로드되지 않도록 예외 차단 규칙을 생성함.
2. **원격 저장소에서 `.env` 삭제**:
   * 로컬 PC 파일 시스템 상의 `.env` 파일은 그대로 보존하면서, Git 캐시에서만 제거하기 위해 아래 명령어를 수행함.
     ```bash
     git rm --cached .env
     ```
3. **변경 내역 푸시**:
   * `.gitignore` 수정본과 `.env` 캐시 제거 상태를 GitHub 원격 저장소에 커밋 및 푸시하여, 이후에 유출이 추가로 진행되는 경로를 원천적으로 막음.

### 단계 2: 신규 Publishable API 키 발급 및 로컬/배포 서버 무중단 교체
1. **신규 API 키 발급**:
   * Supabase 대시보드에서 Legacy 방식의 구형 API 키를 대체하는 현대적인 보안 방식의 **Publishable API Key**를 신규 생성함.
   * **신규 발급 키 값**: `sb_publishable_hu_AavJ7YY0ajj6zZNyQYQ_DAbPwqMP`
2. **로컬 환경 적용**:
   * 로컬 PC의 [`.env`](./.env) 파일 내 `VITE_SUPABASE_ANON_KEY` 값을 기존 유출된 키에서 새로 발급받은 Publishable key로 변경 적용함.
3. **운영 배포 서버(Vercel) 환경 변수 변경 및 재배포**:
   * 운영 환경인 Vercel 대시보드 설정에 진입하여 `VITE_SUPABASE_ANON_KEY` 환경 변수 값을 위 신규 키로 업데이트함.
   * Vercel의 최신 빌드 이력을 활용해 **재배포(Redeploy)**를 즉시 실행하여, 새 키가 적용된 웹사이트를 빌드함.
   * 이 과정에서 기존 키가 일정 기간 동안 유효하도록 설정하여, 재배포 도중에도 서비스가 끊김이나 오류 없이 **Downtime 0초로 무중단 적용**을 성공함.

### 단계 3: 유출된 구형 Legacy API 키 최종 무효화(차단)
1. **구형 키 비활성화**:
   * 신규 키 배포 완료 후 웹사이트의 동작(조회, 작성)이 원활한 것을 확인한 뒤, Supabase 대시보드의 `Legacy anon, service_role API keys` 항목에서 **[Disable JWT-based API keys]** 처리를 실행함.
   * 이로 인해 퍼블릭 GitHub 커밋 이력에 과거 기록으로 박혀 있던 노출된 구형 API 키는 즉시 무효화되어 침투 도구로서의 효력을 상실함.

---

## 3. 최종 상태 요약

| 점검 항목 | 상태 | 내용 |
| :--- | :---: | :--- |
| **소스 동기화 상태** | **정상 (일치)** | 로컬 PC의 핵심 소스 코드는 GitHub와 완벽히 일치합니다. |
| **`.env` 보안 차단** | **보안 완료** | `.gitignore`에 등록되어 더 이상 GitHub에 공유되지 않습니다. |
| **사용 중인 API 키** | **안전 (신규 키)** | 새로 발급된 신규 Publishable 키만 활성화되어 동작 중입니다. |
| **유출된 구형 API 키** | **차단됨 (Disable)** | 기존 유출 키는 비활성화되어 해킹에 악용될 우려가 없습니다. |
| **현재 서비스 운영** | **정상 가동** | 청소년 이용자 서비스의 멈춤이나 오류 없이 무중단 조치되었습니다. |

---
**작성일**: 2026년 6월 10일  
**작성자**: Antigravity (AI Coding Assistant)
